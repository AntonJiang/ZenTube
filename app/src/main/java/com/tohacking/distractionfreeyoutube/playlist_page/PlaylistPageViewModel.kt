package com.tohacking.distractionfreeyoutube.playlist_page

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class PlaylistPageViewModel(app: Application) : AndroidViewModel(app)